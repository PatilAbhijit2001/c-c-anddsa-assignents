#include <stdio.h>
    int main()
    {
        int digit, x;
        printf("Enter a digit\n");
        scanf("%d", &digit);
        x = digit / 10;
        printf("The digit is %d",x);
        return 0;
    }

